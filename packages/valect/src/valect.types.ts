export type VtForm = HTMLFormElement & {
  // TODO form.values ??? maybe not, they can read from the valect element
  // vtValues: Record<string, Record<string, string | string[]>>;
};

export type VtFieldSet = HTMLFieldSetElement;
